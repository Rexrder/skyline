#include <stdio.h>
#include "skylib.h"

int main(){
    int nbuilds = 0;
    int allB[5000][3] = {0};
    int maxBuild = 0;

    nbuilds = inputBuild(allB);
    printf("%d, %d", allB[i][0],allB[i][1]);

    for (int i = 1; i < nbuilds; i++)
    {
        
    }
    return 0;
}